# Kylin-TARS 最小化依赖（uitars作为外部API）
# 适用于：虚拟机/实机安装Kylin操作系统
# 说明：uitars推理服务作为外部API，不需要vLLM/CUDA相关依赖

# ============================================================
# 核心依赖（必需）
# ============================================================

# Web UI框架
gradio>=4.0.0

# 系统监控
psutil>=5.9.0

# DBus通信（系统级，需先安装系统包）
# 系统包：sudo apt install python3-dbus python3-gi gir1.2-gtk-3.0
dbus-python>=1.2.0
PyGObject>=3.42.0

# HTTP请求（调用uitars API）
requests>=2.28.0

# JSON处理
json5>=0.9.0

# 图像处理
Pillow>=9.0.0

# 网络图可视化（记忆轨迹可视化）
networkx>=3.0
matplotlib>=3.6.0

# 日期时间处理
python-dateutil>=2.8.0

# 模糊匹配检索（记忆检索）
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.12.0

# ============================================================
# 可选依赖（功能增强）
# ============================================================

# 语义检索（如果使用sentence-transformers，可选）
# sentence-transformers>=2.2.0  # 仅当启用语义检索时需要

# ============================================================
# 已排除的依赖（uitars作为外部API，不需要）
# ============================================================
# - vllm (推理服务在外部)
# - torch, torchvision, torchaudio (深度学习框架)
# - transformers (模型加载)
# - accelerate (模型加速)
# - nvidia-* (CUDA相关)
# - cuda-* (CUDA相关)
# - cupy (GPU计算)
# - triton (GPU内核)
# - ray (分布式计算)
# - flashinfer (推理优化)

